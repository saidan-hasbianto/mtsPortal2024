import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'mtsPortal2024',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
